#staff-account-edit
    .block-header 修改员工
    .staff-form
        p.form-description 带 * 号的为必填项
        .input-field
             p 员工名字 *
             input(type="text" name="name")
        .input-field
            p 性别 *
            select(name="gender")
                option(value="male") 男
                option(value="female") 女
        .input-field
            p 手机 *
            input(type="text" name="phone" placeholder="手机号将作为登录账号")
        .input-field
            p 密码 *
            input(type="password" name="password" value="123456")
            button.reset-password-btn 重置密码
        .input-field
            p 角色 *
            select(name="role")
                option(value="default") 请选择角色
                option(value="") 管理员
                option(value="") 店长
                option(value="") 财务
                option(value="") 服务员
            button.manage-permission-btn 管理权限
     .btn-container
         button.cancel-btn 取消
         button.save-btn 保存